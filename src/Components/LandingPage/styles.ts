import styled from 'styled-components';

const ProgressBar = styled.div`
  position: fixed;
  top: 0;
  left: 0;
  bottom: 0;
  width: 10px;
  background: linear-gradient(to bottom, blue, cyan, magenta, purple);
  transform-origin: 0% 0%;
`;

export {ProgressBar}
